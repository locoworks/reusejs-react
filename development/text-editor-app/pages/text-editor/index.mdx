---
HEAD OBJECT
title: "ReuseJS text-editor"
author: "Upneet Kaur Hunjan"
---
import Example from "../../components/text-editor/Example/Example.tsx";
import PrepopulatedTextEditor from "../../components/text-editor/PrepopulatedTextEditor/PrepopulatedTextEditor.tsx";

## Text-Editor

## Overview

The `TextEditor` component is a rich text editor built using the Lexical Composer library. It provides a user-friendly interface for editing and formatting text, supporting features such as mentions, links, tables, images and more.

## Installation

To use the `TextEditor` component in your project, you can install it via npm or yarn:

```bash
npm install @locoworks/reusejs-react-text-editor
# or
yarn add @locoworks/reusejs-react-text-editor
```

## External Dependencies

The `TextEditor` component relies on external dependencies - `tailwind-merge`, `lexical` and `@lexical/react`.

```bash
npm install lexical @lexical/react tailwind-merge
# or
yarn add lexical @lexical/react tailwind-merge
```

## Importing

Import the `TextEditor` component into your React application as follows:

```jsx
import { TextEditor } from "@locoworks/reusejs-react-text-editor";
```

## Types/ Exported Components

The `TextEditor` component accepts the following props:

- `useMentionLookupService`: A function to handle mention lookup and return results.
- `convertFileToImageUrl`: A function to convert files to image URLs and return imageUrls.
- `onChangeCallback`: An optional callback for editor reference changes and payload.
- `wrapperClass`: An optional class for styling the editor wrapper.
- `editable`: A boolean indicating the edit state of the editor.
- `setEditable`: A function to set the editable state of the editor. It is a React state setter function that accepts a boolean value.
- `prePopulate`: An optional function to set the intialState of the editor.

## Usage/Examples

### TextEditor demo
<Example/>
```js path=../../components/text-editor/Example name=Example
```

### PrepopulatedTextEditor 
<PrepopulatedTextEditor/>
```js path=../../components/text-editor/PrepopulatedTextEditor name=PrepopulatedTextEditor
```

## Styling

The styling of the editorWrapper can be customized using Tailwind CSS .

> Note: To use the styling of the editor component , you must import the StyleSheet in your application.
```jsx
import "@locoworks/reusejs-react-text-editor/css";
```

## Best Practices

- Ensure that necessary dependencies are installed.
- Handle editor reference changes and payload using the onChangeCallback prop.

## Props Table

| Prop                  | Type                                 | Description                                                                                                       |
| --------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| `useMentionLookupService` | `(mentionString: string \| null) => Array<{ mentionName: string; label: string; }>`                             | A function to handle mention lookup and return results.                                                           |
| `convertFileToImageUrl`   | `(files: FileList \| null) => string \| null`                             | A function to convert files to image URLs and return image URLs.                                                  |
| `onChangeCallback`       | `(editorRef: LexicalEditor \| null, payload: any) => void`                             | An optional callback for editor reference changes and payload.                                                    |
| `wrapperClass`           | `String`                               | An optional class for styling the editor wrapper.                                                                  |
| `editable`               | `Boolean`                              | A boolean indicating the edit state of the editor.                                                                |
| `setEditable`            | `React.Dispatch<React.SetStateAction<boolean>>`                             | Sets the editable state of the editor.                        |
| `prePopulate`            | `() => void`                             | An optional function to set the initial state of the editor.                                                       |

## Notes
>The TextEditor component uses the Lexical library for rich text editing.
Check the Lexical Composer documentation for advanced configuration options.

export default ({ children }) => {
  return <div className="prose max-w-full mx-4 pb-10" >{children}</div>
};
